# Skill-Up Plugin Settings
# ========================

# 저장 모드: local (otel은 현재 사용 안 함)
STORAGE_MODE="local"

# === Local 모드 설정 ===
LOCAL_STORAGE_DIR="$HOME/.claude/.skill-evaluator"

# === 공통 설정 ===
# 디버그 모드 (stderr로 로그 출력)
SKILL_EVAL_DEBUG=false
